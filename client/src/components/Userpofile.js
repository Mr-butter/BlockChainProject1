import React, { Fragment } from "react";
import { withRouter } from "react-router-dom";

function Userpofile(props) {
    return (
        <Fragment>
            <h2>유저 프로필</h2>
        </Fragment>
    );
}

export default withRouter(Userpofile);
